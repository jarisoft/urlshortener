; This configuration file is used to store information such as database 
; connection details

[db]
username = jackjack
password = jjjack
host = localhost
database = urlshortener

[shortener]
targetURL = http://shortener.local
; if following line is off we do 
; not allow redirection to a redirecting page
redirect = on